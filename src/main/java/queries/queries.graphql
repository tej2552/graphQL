#name: getGameById
query ($gameId: ID!, $shouldInclude: Boolean!) {
  game(id: $gameId) {
    id
    title
    platform

    reviews @include(if: $shouldInclude){
      id
      rating
      author {
        name
      }
    }
  }
}